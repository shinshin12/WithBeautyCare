<nav class="navbar navbar-dark bg-dark p-3 "> 
 <div class="container">

     <div class="container navbar-item border-bottom position-relative">
        <% if user_signed_in? %>
      <p class="navbar-brand text-white container">ようこそ<%= current_user.nickname %>さん<i class="far fa-laugh"></i></p> 


         <%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: "font-weight-bold text-white position-absolute", style:"top:10px; left:1000px;"%>
          <% else%>
         <%= link_to 'ログイン', new_user_session_path, class: "font-weight-bold text-white container"%>
         <%= link_to '新規登録', new_user_registration_path, class: "font-weight-bold text-white text-center"%>
          <% end%>
      </div> 

       <div class="mt-1">
           <% if user_signed_in? %>  
        <%= link_to "マイページ", user_path(current_user.id), class: "font-weight-bold text-white text-center ml-5 "%> 
           <% end %>  
         </div>

       <div class="mt-3">
         <%= form_with( url:search_informations_path, local:true, method: :get, class:"search-form") do |f|%>
         <%= f.text_field :keyword %>
         <%= f.submit "検索", class: "btn btn-outline-light"%>
         <% end %>  
         </div>
    </div>  
</nav>
